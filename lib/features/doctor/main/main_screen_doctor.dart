import 'package:flutter/material.dart';

class MainScreenDoctor extends StatelessWidget {
  const MainScreenDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Main Screen Doctor'),
      ),
      
    );
  }
}